@model IEnumerable<završniRad.Models.Student>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prezime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Ime)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Prezime)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.JMBAG }) |
            @Html.ActionLink("Details", "Details", new { id=item.JMBAG }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.JMBAG })
        </td>
    </tr>
}

</table>
